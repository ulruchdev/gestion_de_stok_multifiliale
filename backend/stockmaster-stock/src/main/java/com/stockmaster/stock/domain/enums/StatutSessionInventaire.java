package com.stockmaster.stock.domain.enums;

/** États d'une campagne d'inventaire — {@code DEC-036} (V5 §15) : ouverte, puis validée par l'ADMIN_FILIALE. */
public enum StatutSessionInventaire {
    OUVERTE,
    VALIDE
}
