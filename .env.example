# ============================================================
# StockMaster CM — Variables d'environnement (recopier vers .env)
# ============================================================

# === JWT ===
JWT_SECRET=une-cle-tres-longue-dau-moins-256-bits-pour-hs256

# === Base de données ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=stockmaster_dev
DB_USERNAME=stockmaster
DB_PASSWORD=stockmaster

# === Redis ===
REDIS_HOST=localhost
REDIS_PORT=6379

# === MinIO ===
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=stockmaster
MINIO_SECRET_KEY=stockmaster

# === Mail ===
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=

# === CORS ===
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
